# Arquivos de dados originais (não precisam ir para o repositório)
*.xlsx
*.docx
*.xls

# Firebase CLI
.firebase/
firebase-debug.log

# Sistema operacional
.DS_Store
Thumbs.db
desktop.ini

# Node / JS
node_modules/
.env

# Saída de build do Vite usada só pra gerar/testar localmente — o que é
# servido em produção é o assets/ + index.html na raiz (ver deploy no
# README: hospedagem estática não roda "npm run build" sozinha).
dist/
